# Synthetic cross-source mirror fixtures

This directory contains declarations only. Event bytes in the executable tests are synthetic, except
for the SEC event loaded from the merged PR 2B recorded acceptance golden. No FMP or NVIDIA response
body is copied here. The fixture contract is CC0-1.0 and safe to redistribute.

The cases intentionally distinguish shared artifact bytes from provider provenance. Equal FMP and
issuer-IR primary digests remain two observations and reduce as a mirror duplicate. Equal SEC v2 and
legacy v1 primary digests remain separately actionable because SEC carries a complete evidence bundle.
