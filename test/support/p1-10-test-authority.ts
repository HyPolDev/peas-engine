/** Test-build-only lexical authority copied into the isolated test output after compilation. */
export const P1_10_TEST_AUTHORITY: Readonly<{ kind: "p1-10-test-authority" }> = Object.freeze({
  kind: "p1-10-test-authority",
});
