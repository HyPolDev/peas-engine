/**
 * Ordinary production builds have no first-boot authority. The owned deployment build replaces
 * only its emitted copy before running the explicit runtime initializer.
 */
export const P1_10_PROVISIONING_AUTHORITY: undefined = undefined;
